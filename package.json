{
  "name": "typescript_study",
  "version": "1.0.0",
  "description": "styding typescript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier-format": "prettier --config .prettierrc 'src/**/*.ts' --write"
  },
  "author": "William Pérez",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
